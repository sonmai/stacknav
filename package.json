{
  "name": "stacknav",
  "displayName": "StackNav",
  "description": "Navigate GitHub stacked pull requests from the VS Code status bar.",
  "icon": "media/icon.png",
  "version": "0.1.0",
  "publisher": "sonmai",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/sonmai/stacknav.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/src/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "stacknav.load",
        "title": "StackNav: Load Stack for Current PR"
      },
      {
        "command": "stacknav.loadUrl",
        "title": "StackNav: Load Stack from PR URL\u2026"
      },
      {
        "command": "stacknav.up",
        "title": "StackNav: Up"
      },
      {
        "command": "stacknav.down",
        "title": "StackNav: Down"
      },
      {
        "command": "stacknav.select",
        "title": "StackNav: Select PR\u2026"
      },
      {
        "command": "stacknav.refresh",
        "title": "StackNav: Refresh"
      },
      {
        "command": "stacknav.first",
        "title": "StackNav: Go to First Layer"
      },
      {
        "command": "stacknav.selectStack",
        "title": "StackNav: Select Stack\u2026"
      },
      {
        "command": "stacknav.selectRepository",
        "title": "StackNav: Select Repository\u2026"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -watch -p .",
    "test": "npm run clean && npm run compile && node --test out/test/*.test.js",
    "package:vsix": "vsce package --no-dependencies",
    "vscode:prepublish": "npm run clean && npm run compile",
    "clean": "node -e \"require('node:fs').rmSync('out', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "4.0.0",
    "typescript": "^5.6.0"
  },
  "extensionDependencies": [
    "vscode.git"
  ],
  "extensionKind": [
    "workspace"
  ]
}
